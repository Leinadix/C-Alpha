namespace Test {
    layout Tuple2 {
        int left;
        int right;
    };
};

fn int test2() {
        ->Test.Tuple2 B = ~Test.Tuple2[0];
        B.left = 65;
        B.right = 106;

        ret B.right;
    };

fn int main() {
    ret test2();
};